services:
  - type: web
    name: python-app
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    autoDeploy: false
    healthCheckPath: /
    scaling:
      minInstances: 1
      maxInstances: 1